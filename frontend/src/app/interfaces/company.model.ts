export interface Company {
 // Centro

  id: number;
  name: string;
  cif: string;
  street: string;
  city: string;
  postalCode: string;
  values: string;
  treatments: string;
  active: boolean;
  photoUrl:string;
}
